{
	"name": "portfolio-root",
	"private": true,
	"scripts": {
		"build:studio": "cd backend_sanity && npx @sanity/cli build ./dist --base /studio && rm -rf ../frontend_react/public/studio && cp -r ./dist ../frontend_react/public/studio",
		"build:frontend": "cd frontend_react && npm install && npm run build",
		"build:all": "npm run build:studio && npm run build:frontend"
	}
}
