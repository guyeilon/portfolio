{
	"name": "portfolio-root",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "npm run build:frontend",
		"build:frontend": "cd frontend_react && npm install && npm run build",
		"deploy": "cd frontend_react && npm run deploy"
	},
	"devDependencies": {
		"gh-pages": "^6.3.0"
	}
}
