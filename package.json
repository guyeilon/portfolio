{
	"name": "portfolio-root",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build:studio": "cd backend_sanity && npm run build -- --output ../frontend_react/public/studio --base /studio",
		"build:frontend": "cd frontend_react && npm install && npm run build",
		"build:all": "npm run build:studio && npm run build:frontend"
	}
}
